# 書き方

**何を作るかは Issue が決め、どう書くかをここが決める。**

設計の決定は Issue の本文にある（#1 データ構造、#2 設定、#3 配布、#4 外部インターフェース、
#5 取得経路、#6 画面、#13 スケジューラ、#15 アーキテクチャ、#7 実装の順序）。ここに置くのは
**書き方の規則だけ**にする。

---

## ドキュメント

### 何を書くか

**その形にした理由を書く。** 何をするかはコードが言う。

```rust
// 良い
/// 投影を DROP して作り直し、ログから埋め直す。
///
/// 投影のスキーマを変えたいときは移行ではなくこれを走らせる。移行に混ぜると、
/// いつの間にか投影に手作業の移行が当たって作り直せなくなる。

// 悪い — コードが言っていることの繰り返し
/// item テーブルを DROP して、CREATE して、ログから INSERT する。
```

**決定は Issue 番号で参照する。** 本文をコードへ写すと写しが2つになり、Issue を直しても
コードが古いまま残る経路ができる。

```rust
/// 期限まで配信元に在ることを確かめた。捨ててよい。
///
/// 証を要求するので、期限が過ぎたというだけで捨てる実装は書けない（#1）。
```

**次にここを触る人が間違えることだけ残す。** レビューや相談の場で説明のために作った比較・
言い換え・経緯は、そのときの相手に向けたもの。

```rust
// 悪い — 説明のために作った比較が残っている
/// 識別子の newtype とはそこが違う — あちらは包むのが仕事だが、
/// こちらは作る道を絞るのが仕事。

// 良い
/// 導出は `Display` まで。`Constructor` と `From` はこの2つの道を迂回させるので外してある。
```

**型で締めなかった所は、そう書く。** 「決めて緩めた」と「締め忘れた」を後から区別できる
ようにするための記述で、#7 の決め事が要求している。

### どう書くか

| | |
|---|---|
| **一度だけ書く** | 言い換えて補強したくなったら、弱いほうを消す |
| **肯定形で書く** | 「生の文字列からは作れない」ではなく「作る道は `normalize_item` と `normalize_source` の2つ」 |
| **指しているものの名前で書く** | 「口」「窓」のような比喩は、伝わらないうえに形の違うものを1語へ丸める |
| **要求のレベルで書く** | 「`sqlx` がこう返すから」ではなく「#1 が `NULL` を許す列をこう決めているから」 |
| **推測は結論の隣に置く** | 「たぶん」を別の段落へ追いやると、結論だけが読まれる |

---

## 実装

### 導出と trait で書く

**マクロは他に手段が無いときだけ。** 自由度が高いぶん、書き手の想定を外れた展開が起きても
コンパイラが助けない。`derive`・generics・trait で書ける形は、そちらを採る。

`crates/escrow-domain/tests/macros.rs` が `macro_rules!` の一覧を表で固定している。
足すには表を編集することになるので、必ず差分に現れる。

### ライブラリを採るかどうか

**判定の基準は行数ではない。** 順に見て、どれかで止まったら見送る。

1. **置き換える対象に、テストすべき振る舞いがあるか。**
   分岐・変換・境界・単位があれば採る — 自前だと想定外の入力を自分で洗い出すことになり、
   洗い出しを省いたぶんがそのままバグになる。
   `Self(x.into())` と `&self.0` のように振る舞いが無いものは、行数が減るだけ。
2. **呼ぶ側の負担が変わらないか。**
   呼び出しの綴りが変わる、型注釈が要るようになる、`.to_owned()` が要る — どれも増加。
3. **公式のガイドラインがどちらかを優位と書いているか。**
   書いていなければ現状維持。`new` と `From` は
   [C-CTOR](https://rust-lang.github.io/api-guidelines/predictability.html) と
   [C-CONV-TRAITS](https://rust-lang.github.io/api-guidelines/interoperability.html) の
   どちらも相手を否定しておらず、std 自身が `String::new` と `String::from` の両方を
   持っている。揃っているほうを崩さない。
4. **見送った導出は、理由をその型の doc に1行残す。**
   便利だからと後から足されるのを防ぐ。

### Rust の書き方に従う

**標準の綴りがあるならそれを採る。** `as_str` は std 自身の綴り（`String::as_str`）で、
`AsRef` は generic の境界に使う別の道具。エディションや言語機能の更新で簡潔に書けるように
なったものは、取り込む。

### 同じ仕事は同じ形

1つの仕事に複数の書き方が混ざったら、どれかへ寄せる。

| 仕事 | 形 |
|---|---|
| 値を包む newtype を作る | `X::new(...)` |
| 内側の値を取り出す | `as_str()`（借用）/ `i64::from(x)`（`Copy`） |

これより下の層 — 状態を動かす経路、事象を書く経路 — は下の表が構造で固定しているので、
ここには書かない。

### 規律ではなく構造にする

**「気をつける」で守るものを増やさない。** 守らせたい約束は、破ったときに落ちる形にする。

| 守るもの | 落ちる場所 |
|---|---|
| カーネルの依存とモジュールの一覧 | `crates/escrow-domain/tests/kernel.rs` |
| `macro_rules!` の一覧 | `crates/escrow-domain/tests/macros.rs` |
| crate の依存の向き、スライス同士の隔離 | `crates/escrow-domain/tests/dependency_direction.rs` |
| `escrow-ledger` の公開 API、投影へ書く SQL の置き場所 | `crates/escrow-ledger/tests/public_api.rs` |
| 状態遷移の網羅（9状態 × 11事象のうち 20 本） | `crates/escrow-domain/src/state.rs` |
| 事象の順序と競合 | `item_event(item_id, seq)` の UNIQUE |

---

## 動かす

```sh
cargo fmt --all
cargo clippy --workspace --all-targets -- -D warnings
cargo nextest run --workspace
cargo test --workspace --doc      # nextest は doctest を実行しない
```

SQL を変えたら `.sqlx/` を取り直す。手順は `.cargo/config.toml` の先頭にある。
