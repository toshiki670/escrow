[package]
name = "escrow-app"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

# 段5 の、組み立てる crate（#82）。入口はここだけを見る。
[dependencies]
escrow-domain.workspace = true
escrow-ledger.workspace = true
escrow-config.workspace = true
escrow-scheduler.workspace = true
escrow-acquisition.workspace = true
escrow-transcription.workspace = true
escrow-handover.workspace = true
thiserror.workspace = true

[dev-dependencies]
tempfile.workspace = true
tokio.workspace = true

[features]
# 入口のテストが台帳へ置く形（#82）。入口は `escrow-ledger` を名前で知らないので、
# 仕込みもここが持つ。
fixture = []

[lints]
workspace = true
