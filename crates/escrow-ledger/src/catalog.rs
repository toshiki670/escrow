//! 人が編集する設定 — `person` · `source` · `exclude`（#15）。
//!
//! **事象にしない。** 状態機械を持たず、履歴を残す理由も無いので、いまの値だけを
//! 持つ。事象の側と混ざらないように、集約ではなくこの1つのモジュールにまとめる。
//!
//! ここに置くのは、スライスがそれぞれ `source` を読む SQL を書くのを防ぐため。
//! 同じデータをスライスごとのやり方で読み書きしてずれていくのが Vertical Slice の
//! 定番の失敗で、設定を割ると真っ先にそれを踏む（#15）。

mod exclude;
mod person;
mod source;

pub use source::NewSource;
