[package]
name = "escrow-gui"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

# 依存は、中身が入るときに要るものだけを足す。

[lints]
workspace = true
