//! 預かりの見張り（#15 のスライス）。
//!
//! `holding` の項目を配信元と突き合わせ、期限まで在り続けたものを捨て、消えたものを
//! `kept` へ残す。終端に達した項目の孤児ファイルを掃除するのもここ（#7）。
//!
//! # まだ空
//!
//! 判定そのものは **Phase 6 で入る**（#7）。理由は2つ。
//!
//! - #5 の「期限が過ぎていても、直近の確認で『在る』が取れていなければ捨てない」の
//!   「直近」が何を指すかは、巡回の間隔（#2 の `check.interval_hours`）が決まって
//!   初めて定義できる
//! - 時計を注入したテストで確かめる、と #7 が Phase 6 の受け入れに書いている。
//!   その注入の形より先に判定を書くと、書き直しになる
//!
//! それでも crate として立てておくのは、**スライス同士が互いを知らないことを守る
//! 表に、5つ目の節点が要る**ため。空の crate は場所取りではなく、依存の表の一部。
