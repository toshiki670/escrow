[package]
name = "escrow-tests"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false
autotests = false

# ワークスペース全体にかかる守り。escrow の crate を1つも依存に持たず、読むのは
# `Cargo.toml` とソース、そして規約のファイルだけ。
[lib]
name = "escrow_tests"
path = "workspace.rs"

[dependencies]
toml.workspace = true

[[test]]
name = "dependency_direction"
path = "dependency_direction.rs"

[[test]]
name = "macros"
path = "macros.rs"

[[test]]
name = "rules_index"
path = "rules_index.rs"

[lints]
workspace = true
